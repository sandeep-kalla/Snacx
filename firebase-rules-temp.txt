rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - FOR TESTING ONLY
    // Replace with proper rules after authentication is fixed
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
